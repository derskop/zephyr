# N32 bxCAN driver verification application
#
# The CAN_N32_BXCAN driver is enabled automatically via devicetree
# (DT_HAS_NSING_N32_BXCAN_ENABLED) once can1/can2 are status = "okay".

CONFIG_CAN=y

# Build with "CONFIG_CAN_SAMPLE_CROSS_BUS=y" in a board-specific config
# overlay to run the physical cross-bus test between CAN1 and CAN2 instead of
# the default internal loopback test.
