# Zephyr 开发环境搭建与使用指南（Ubuntu · 新手版）

> 本指南覆盖完整流程：**系统准备 → 工具链升级 → Zephyr 源码与 SDK 安装 → 日常编译**。
> 所有命令按顺序执行即可，`#` 开头为说明注释，可按需选择是否执行。

---

## 快速开始：一键搭建（推荐）

第一、二、三部分的流程已整理成脚本，运行一次即可完成全部环境搭建（自动跳过已完成步骤，可重复执行）：

```bash
bash ~/zephyrproject/setup_zephyr_env.sh
```

- 中途会询问 **sudo 密码**（系统更新/安装软件需要）
- 首次运行下载 Zephyr SDK（约 1GB），需十几分钟；网络慢或下载失败时，编辑脚本顶部 `PROXY=""` 填入代理地址
- 脚本顶部配置区可改：`MANIFEST_URL`（Zephyr 仓库）、`UPDATE_MODE`（`full`=拉取全部 / `nsing`=只拉 nsing 驱动库 / `skip`=暂不拉取）
- 跑完后重新打开终端，再按**第四部分**进行开发；脚本有疑问时，对照下面第一、二、三部分逐条核对

---

## 第一部分：系统基础准备

### 1.1 更新系统

```bash
sudo apt update
sudo apt upgrade
```

### 1.2 安装编译依赖

```bash
sudo apt install --no-install-recommends git cmake ninja-build gperf \
  ccache dfu-util device-tree-compiler wget python3-dev python3-venv python3-tk \
  xz-utils file make gcc gcc-multilib g++-multilib libsdl2-dev libmagic1
```

### 1.3 检查依赖版本（最低要求）

| 工具 | 检查命令 | 最低版本 |
|------|----------|----------|
| CMake | `cmake --version` | 3.28.0 |
| Python | `python3 --version` | 3.12 |
| DTC | `dtc --version` | 1.4.6 |

> 版本满足要求则直接跳到**第三部分**；不满足才需要执行第二部分的升级操作。

---

## 第二部分：工具链升级

### 2.1 更新 CMake（使用 Kitware 官方源）

```bash
# 添加 Kitware 的签名密钥
wget -O - https://apt.kitware.com/keys/kitware-archive-latest.asc 2>/dev/null | sudo apt-key add -

# 添加仓库（针对 Ubuntu 22.04，若不同请替换版本代号）
sudo apt-add-repository "deb https://apt.kitware.com/ubuntu/ $(lsb_release -cs) main"
sudo apt update

# 安装或升级 CMake
sudo apt install cmake
```

```bash
# 检查版本
cmake --version
```

### 2.2 升级 Python 到 3.12（pyenv 方式）

```bash
# 1. 安装 pyenv 依赖（若尚未安装）
sudo apt update
sudo apt install -y make build-essential libssl-dev zlib1g-dev libbz2-dev \
libreadline-dev libsqlite3-dev wget curl llvm libncursesw5-dev xz-utils \
tk-dev libxml2-dev libxmlsec1-dev libffi-dev liblzma-dev

# 2. 设置代理（地址与端口按实际情况填写）
export http_proxy="http://代理地址:端口"
export https_proxy="http://代理地址:端口"

# 3. 安装 pyenv
curl https://pyenv.run | bash

# 4. 将以下三行添加到 ~/.bashrc（或 ~/.zshrc）
export PATH="$HOME/.pyenv/bin:$PATH"
eval "$(pyenv init -)"
eval "$(pyenv virtualenv-init -)"   # 可选

# 5. 重新加载配置
source ~/.bashrc

# 6. 安装 Python 3.12.5（或最新补丁）
pyenv install 3.12.5

# 7. 设为全局默认
pyenv global 3.12.5

# 8. 验证
python3 --version   # 应显示 3.12.5
```

---

## 第三部分：获取 Zephyr 并安装 Python 依赖项

### 3.1 创建虚拟环境

```bash
# 创建一个新的虚拟环境
python3 -m venv ~/zephyrproject/.venv
```

```bash
# 激活虚拟环境（激活后，您的 shell 名称将带有前缀 (.venv)）
source ~/zephyrproject/.venv/bin/activate
```

```bash
# 停用虚拟环境
deactivate
```

### 3.2 安装 west

```bash
pip install west
```

### 3.3 获取 Zephyr 源代码

```bash
west init -m https://github.com/derskop/zephyr.git ~/zephyrproject
cd ~/zephyrproject

# 可以先不执行，用于下载不同厂商 HAL，可以在使用时按需配置项目组下载，可参考
# https://docs.zephyrproject.org/latest/develop/west/manifest.html#west-manifest-groups
west update

# 或者使用下面的指令，只拉取 nsing 驱动库
west update hal_nsing
```

### 3.4 安装 Zephyr 的 Python 依赖项

```bash
west packages pip --install
```

```bash
# 更新 pip
pip install --upgrade pip
```

### 3.5 导出 Zephyr CMake 包

```bash
west zephyr-export
```

### 3.6 安装 Zephyr SDK

```bash
cd ~/zephyrproject/zephyr
west sdk install
```

### 3.7 配置 SDK 环境变量

```bash
# 将 SDK 路径设置为环境变量并导出到系统中（注意：用户名需要根据自己的电脑填写）
echo 'export ZEPHYR_SDK_INSTALL_DIR=/home/用户名/zephyr-sdk-1.0.1' >> ~/.profile
echo 'export ZEPHYR_SDK_INSTALL_DIR=/home/用户名/zephyr-sdk-1.0.1' >> ~/.bashrc
```

```bash
# 验证导出成功（应输出 /home/nsing/zephyr-sdk-1.0.1，.profile 生效）
bash -lc 'echo $ZEPHYR_SDK_INSTALL_DIR'
```

---

## 第四部分：每次开发前——激活环境与编译

### 4.1 激活虚拟环境

```bash
source ~/zephyrproject/.venv/bin/activate
```

### 4.2 west build 命令详解

把 west build 拆开讲透，其实就一件事：告诉 west"用哪个板子、编译哪个应用的代码、结果放哪"。

**① 命令模板**

```bash
west build -b <板子> <应用目录> -d <输出目录> -p
```

| 部分 | 含义 | 示例 |
|------|------|------|
| `-b <板子>` | 目标开发板（配置文件来自 zephyr/boards/） | `-b n32g45xml_stb` |
| `<应用目录>` | 你的代码在哪（包含 CMakeLists.txt 的目录） | `zephyr/samples/basic/blinky` 或你自己的应用目录 |
| `-d <输出目录>` | 编译产物放哪（.elf/.hex 生成的地方） | `-d build-gpio` |
| `-p` | 清除之前的东西重新编译（防止残留配置干扰） | 不加 = 增量编译 |

**② 三个"目录"不要搞混**

| 目录 | 是谁 | 位置 |
|------|------|------|
| 应用目录 | 源码（你写的 main.c 所在处） | 命令里第二个参数 |
| 输出目录 | 编译结果（zephyr.elf 所在处） | `-d` 参数，默认是当前目录下的 `build/` |
| 当前工作目录 | 你敲命令时所在的目录 | 命令 `cd` 到哪就是哪 |

**③ 你最常用的三种场景**

```bash
# 场景 A：编译 Zephyr 自带样例，验证板子能不能跑
west build -b n32g45xml_stb -p zephyr/samples/basic/blinky

# 场景 B：编译你自己的应用（以后主要用这个）
west build -b n32g45xml_stb -p /home/nsing/my_app

# 场景 C：改了代码，重新编译（同场景 B 命令，会增量编译，很快）
west build -b n32g45xml_stb /home/nsing/my_app
```

**④ 什么时候必须加 `-p`？**

- 换板子 / 换应用 / 换配置时：必须 `-p`（否则沿用旧配置，编译出来不对）
- 只是改了 `.c` 代码：不用 `-p`（增量编译，几秒钟）
- 拿不准就加 `-p`，最多多花几十秒，绝不会错

**⑤ 编译完去哪找固件**

```bash
# 在输出目录下（默认 build/，或你 -d 指定的目录）
build/zephyr/zephyr.elf     # 调试用（符号）
build/zephyr/zephyr.hex     # 烧录用
build/zephyr/zephyr.bin     # 烧录用
```

**⑥ 清理编译文件（等价 make clean）**

```bash
# 清除编译产物（.o / .elf 等），保留 CMake 配置，下次编译不用重新配置
west build -t clean -d build
```

```bash
# 全部清空（连配置一起删，等价 make distclean；换应用/换板子时用）
west build -t pristine -d build
# 或者直接删目录，效果相同
rm -rf build
```

> 日常最省事的是不单独清：编译时加 `-p` 自动清空重建（见 ④）。

**⑦ 一条完整流程（你的实际场景）**

```bash
cd /home/nsing/zephyrproject
source .venv/bin/activate
export ZEPHYR_SDK_INSTALL_DIR=/home/nsing/zephyr-sdk-1.0.1

# 1. 先跑通板子：编译 blinky，产物放到 build-gpio
west build -b n32g45xml_stb -p zephyr/samples/basic/blinky -d build-gpio

# 2. 之后写自己的 GPIO 应用，编译产物继续放 build-gpio
west build -b n32g45xml_stb -p /home/nsing/my_app -d build-gpio

# 3. 每次改完代码就重跑（不带 -p，增量）
west build -b n32g45xml_stb /home/nsing/my_app -d build-gpio
```

---

## 第五部分：每次开发完成

```bash
# 停用虚拟环境
deactivate
```

---

## 第六部分：west 命令补充详解

### 6.1 west 是什么

west（Zephyr's meta-tool）是 Zephyr 的官方元工具：它管理多个 Git 仓库（manifest 机制）、执行构建与烧录。你在 Zephyr 开发中 90% 的命令都走它，格式统一为：

```bash
west <子命令> [选项]
```

### 6.2 帮助与全局信息

```bash
west -h                    # 查看所有子命令
west build -h              # 查看 build 子命令的帮助（每个子命令都支持 -h）
west topdir                # 打印顶层目录（你的 zephyrproject 路径）
west list                  # 列出 manifest 管理的所有仓库
west status                # 查看各仓库的 Git 状态（谁改了什么）
west config build.board    # 查看当前配置的默认板子
```

### 6.3 常用子命令速查表

| 子命令 | 作用 | 示例 |
|--------|------|------|
| `west init` | 初始化工作区（生成 west.yml） | `west init -m <仓库地址> ~/zephyrproject` |
| `west update` | 按 west.yml 拉取/更新全部仓库 | `west update` |
| `west build` | 编译应用 | `west build -b n32g45xml_stb -p zephyr/samples/basic/blinky` |
| `west flash` | 烧录固件到板子 | `west flash -d build` |
| `west zephyr-export` | 导出 Zephyr CMake 包 | `west zephyr-export` |
| `west sdk install` | 下载并安装 Zephyr SDK | `west sdk install` |
| `west packages pip --install` | 安装 Zephyr 的 Python 依赖 | `west packages pip --install` |
| `west manifest --resolve` | 查看解析后的完整 manifest | `west manifest --resolve` |

### 6.4 源码管理：init / update

```bash
# 初始化：把 manifest 仓库克隆下来并创建工作区
west init -m https://github.com/derskop/zephyr.git ~/zephyrproject

# 更新：按 west.yml 拉取所有项目（第一次最慢）
west update

# 只更新某一个项目（如只拉 nsing 驱动库，速度快）
west update hal_nsing

# 一次更新多个项目
west update hal_nsing zephyr
```

> **关于 HAL 按需下载**：west.yml 通过 `groups` 划分仓库组（如 `hal` 组）。未启用的组不会被 `west update` 拉取，按需启用即可，详见：
> https://docs.zephyrproject.org/latest/develop/west/manifest.html#west-manifest-groups

### 6.5 构建：west build 参数详解

```bash
west build [选项] <应用目录>
```

| 选项 | 含义 | 示例 |
|------|------|------|
| `-b <板子>` | 指定目标开发板 | `-b n32g45xml_stb` |
| `-d <目录>` | 构建输出目录（默认 `build/`） | `-d build` / `-d build-gpio` |
| `-p` | 清空构建目录后重新编译（pristine） | `-p` / `-p auto` |
| `-t <目标>` | 执行指定构建目标（见下方） | `west build -t menuconfig` |
| `-c` | 只做 CMake 配置，不编译 | `west build -c -b n32g45xml_stb ...` |
| `-v` | 显示详细编译输出 | `west build -v` |

**`-p` 的三种取值**

| 写法 | 行为 | 什么时候用 |
|------|------|------------|
| `-p`（或 `-p always`） | 每次都清空重建 | 拿不准时，绝不会错 |
| `-p auto` | 配置变化时自动清空 | 推荐日常使用 |
| 不加 `-p` | 增量编译 | 只改了 `.c` 代码，想快 |

**常用构建目标（`-t`）**

```bash
west build -t menuconfig    # 图形化配置内核（最常用）
west build -t flash         # 编译并烧录（等价于 west flash，需先编译过）
west build -t rom_report    # 查看 ROM 占用
west build -t ram_report    # 查看 RAM 占用
```

### 6.6 menuconfig：可视化配置内核（Kconfig）

Zephyr 的配置体系和 Linux 内核同源（都是 Kconfig），提供两种可视化配置界面（安装依赖里的 `python3-tk` 就是给 guiconfig 用的）：

**① 打开配置界面（两种任选）**

```bash
# 前提：必须先成功编译一次（menuconfig 读取 build 目录里的配置）
west build -b n32g45xml_stb -p zephyr/samples/basic/blinky -d build

# 终端界面（最常用）
west build -t menuconfig -d build

# 图形界面（带鼠标的窗口）
west build -t guiconfig -d build
```

**② 常用按键操作**

| 按键 | 作用 |
|------|------|
| 空格 | 勾选 / 取消勾选 |
| 回车 | 进入子菜单 |
| `/` | 搜索配置项 |
| `?` | 查看当前项帮助 |

**③ 保存改动（关键，新手常卡在这）**

menuconfig 里改的配置默认只存在 `build/` 里，重新编译或换目录就丢了。标准做法：

```bash
# 进入菜单 → "Save minimal config" → 文件名填 prj.conf → 退出
west build -t menuconfig -d build

# 把生成的 prj.conf 放到你的应用目录下（和 main.c 同级）
```

`Save minimal config` 只保存你改过的项（生成最小配置），之后每次编译会自动应用 `prj.conf`，不用再进界面。

### 6.7 烧录：west flash

```bash
# 烧录 build 目录下的固件
west flash -d build

# 指定烧录器（runner）：J-Link 对应的 runner 是 jlink
west flash -d build --runner jlink

# 查看当前板子支持的 runner 列表
west flash -d build --help
```

> 团队实际使用中，也可直接用 VS Code 里的 flash 任务（内部走 JLinkExe），效果相同。

### 6.8 环境与依赖

```bash
# 安装 Zephyr 的 Python 依赖（搭建环境时执行一次）
west packages pip --install

# 导出 Zephyr CMake 包（供 CMake 找到 Zephyr）
west zephyr-export

# 安装 Zephyr SDK（-d 可指定安装目录，默认 ~/zephyr-sdk）
west sdk install
```

### 6.9 日常组合流程

```bash
cd ~/zephyrproject
source .venv/bin/activate

# 编译
west build -b n32g45xml_stb -p zephyr/samples/basic/blinky -d build

# 烧录
west flash -d build
```

### 6.10 排错提示

1. **报 "unknown board"** → `-b` 写错了，用 `west boards` 查看可用板子，或确认板子名（`n32g45xml_stb`）
2. **报 "Build directory ... is for application ..."** → 构建目录被别的应用占用了，加 `-p` 或删掉 build 目录
3. **命令找不到（west: command not found）** → 虚拟环境没激活：先 `source ~/zephyrproject/.venv/bin/activate`
4. **不确定某个命令怎么用** → 先 `west <子命令> -h`，比查文档快
